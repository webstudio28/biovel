<!DOCTYPE html>
<html lang="bg">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>
            {% if title %}{{ title }} | {% endif %}Вега Тест - Холистична Диагностика
        </title>
        <meta name="description" content="{% if description %}{{ description }}{% else %}ВЕГА ТЕСТ - компютърна диагностика за здравословно хранене с най-подходящите храни за вашия организъм{% endif %}">
        
        <!-- Fonts -->
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700&family=Playfair+Display:wght@400;500;600;700&display=swap" rel="stylesheet">

        <!-- Stylesheets -->
        <link rel="stylesheet" href="{{ '/assets/css/styles.css' | url }}">
        <link rel="stylesheet" href="{{ '/assets/css/custom-styles.css' | url }}">
        <link rel="stylesheet" href="{{ '/assets/css/home.css' | url }}">

        <!-- Open Graph meta tags -->
        <meta property="og:title" content="{% if title %}{{ title }}{% else %}Вега Тест - Холистична Диагностика{% endif %}"/>
        <meta property="og:description" content="{% if description %}{{ description }}{% else %}ВЕГА ТЕСТ - компютърна диагностика за здравословно хранене{% endif %}"/>
        <meta property="og:type" content="website"/>

        <!-- External Scripts -->
        <script src="https://unpkg.com/@lottiefiles/lottie-player@latest/dist/lottie-player.js"></script>
        <script>
            if (window.gsap && window.ScrollTrigger) {
            gsap.registerPlugin(ScrollTrigger);
          }
          
          // Mobile menu toggle
          document.addEventListener('DOMContentLoaded', function() {
              const mobileMenuToggle = document.getElementById('mobile-menu-toggle');
              const mobileMenu = document.getElementById('mobile-menu');
              const body = document.body;
              
              if (mobileMenuToggle && mobileMenu) {
                  mobileMenuToggle.addEventListener('click', function() {
                      // Toggle burger animation
                      this.classList.toggle('active');
                      
                      // Toggle mobile menu
                      if (mobileMenu.classList.contains('translate-x-full')) {
                          mobileMenu.classList.remove('translate-x-full');
                          mobileMenu.classList.add('translate-x-0');
                          body.style.overflow = 'hidden'; // Prevent body scroll
                      } else {
                          mobileMenu.classList.add('translate-x-full');
                          mobileMenu.classList.remove('translate-x-0');
                          body.style.overflow = ''; // Restore body scroll
                      }
                  });
              }
              
              // Mobile accordion functionality
              const accordionToggles = document.querySelectorAll('.mobile-accordion-toggle');
              accordionToggles.forEach(toggle => {
                  toggle.addEventListener('click', function() {
                      const targetId = this.getAttribute('data-target');
                      const targetElement = document.getElementById(targetId);
                      const icon = this.querySelector('svg');
                      
                      if (targetElement) {
                          const isExpanded = targetElement.style.maxHeight && targetElement.style.maxHeight !== '0px';
                          
                          // Close all other accordions first
                          accordionToggles.forEach(otherToggle => {
                              if (otherToggle !== toggle) {
                                  const otherTargetId = otherToggle.getAttribute('data-target');
                                  const otherTargetElement = document.getElementById(otherTargetId);
                                  const otherIcon = otherToggle.querySelector('svg');
                                  if (otherTargetElement) {
                                      otherTargetElement.style.maxHeight = '0px';
                                      otherIcon.style.transform = 'rotate(0deg)';
                                  }
                              }
                          });
                          
                          if (isExpanded) {
                              // Collapse current
                              targetElement.style.maxHeight = '0px';
                              icon.style.transform = 'rotate(0deg)';
                          } else {
                              // Expand current
                              targetElement.style.maxHeight = targetElement.scrollHeight + 'px';
                              icon.style.transform = 'rotate(180deg)';
                          }
                      }
                  });
              });
          });
        </script>

        <!-- Home Animations -->
        <script src="{{ '/assets/js/home-animations.js' | url }}"></script>
        {% if page.url == "/" %}
        <script src="{{ '/assets/js/approach-animations.js' | url }}"></script>
        {% endif %}

        <script src="{{ '/assets/js/animations.js' | url }}"></script>


    </head>
    <body class="text-primary-volcanic font-['Montserrat']">
        <!-- Background Gradie font-oswaldnt Container -->
        <div class="fixed inset-0 bg-gradient-to-br from-primary-ceramic  to-primary-ceramic -z-20"></div>
        
        <!-- Background Image Container -->
        <div class="fixed opacity-20 inset-0 bg-[url('/assets/images/base-bg.jpg')] bg-cover bg-center bg-no-repeat -z-10"></div>
        
        <!-- Header -->
        <header class="fixed w-full z-50 bg-transparent backdrop-blur-sm border-b border-transparent transition-all duration-300" id="header">
            <nav class="max-w-7xl mx-auto flex justify-between items-center h-20 px-4">
                <div class="flex items-center gap-8">
                    <a href="/" class="text-2xl font-bold text-primary-evergreen font-['Playfair_Display']">
                        <span class="text-primary-evergreen">Вега</span>Тест
                    </a>
                    <div class="hidden lg:flex lg:items-center space-x-8">
                        <a href="/" class="hover:text-primary-evergreen transition-colors font-medium text-primary-volcanic">Начало</a>
                        <a href="/za-men/" class="hover:text-primary-evergreen transition-colors font-medium text-primary-volcanic">За мен</a>
                        <div class="relative group">
                            <a href="/uslugi/" class="hover:text-primary-evergreen transition-colors font-medium text-primary-volcanic inline-flex items-center gap-1">Услуги
                                <svg class="w-4 h-4 opacity-70" viewBox="0 0 20 20" fill="currentColor"><path fill-rule="evenodd" d="M5.23 7.21a.75.75 0 011.06.02L10 10.94l3.71-3.71a.75.75 0 111.06 1.06l-4.24 4.24a.75.75 0 01-1.06 0L5.21 8.29a.75.75 0 01.02-1.08z" clip-rule="evenodd"/></svg>
                            </a>
                            <div class="absolute top-full left-0 w-64 rounded-xl bg-white shadow-xl border border-primary-dune/20 opacity-0 pointer-events-none group-hover:opacity-100 group-hover:pointer-events-auto transition">
                                <div class="py-2 mt-2">
                                    {% for s in services %}
                                    <a href="/uslugi/{{ s.slug }}/" class="block px-4 py-2 text-sm text-primary-volcanic hover:bg-primary-sand/50">{{ s.name }}</a>
                                    {% endfor %}
                                    <div class="px-4 py-2 border-t border-primary-dune/20">
                                        <a href="/uslugi/" class="text-sm text-primary-evergreen font-medium">Виж всички услуги →</a>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="relative group">
                            <a href="/produkti/" class="hover:text-primary-evergreen transition-colors font-medium text-primary-volcanic inline-flex items-center gap-1">Продукти
                                <svg class="w-4 h-4 opacity-70" viewBox="0 0 20 20" fill="currentColor"><path fill-rule="evenodd" d="M5.23 7.21a.75.75 0 011.06.02L10 10.94l3.71-3.71a.75.75 0 111.06 1.06l-4.24 4.24a.75.75 0 01-1.06 0L5.21 8.29a.75.75 0 01.02-1.08z" clip-rule="evenodd"/></svg>
                            </a>
                            <div class="absolute top-full left-0 w-64 rounded-xl bg-white shadow-lg border border-primary-dune/20 opacity-0 pointer-events-none group-hover:opacity-100 group-hover:pointer-events-auto transition">
                                <div class="py-2 mt-2">
                                    {% for product in products %}
                                    {% if loop.index <= 6 %}
                                    <a href="/produkti/{{ product.meta_title }}/" class="block px-4 py-2 text-sm text-primary-volcanic hover:bg-primary-sand/50">{{ product.title }}</a>
                                    {% endif %}
                                    {% endfor %}
                                    <div class="px-4 py-2 border-t border-primary-dune/20">
                                        <a href="/produkti/" class="text-sm text-primary-evergreen font-medium">Виж всички продукти →</a>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <a href="/kontakti" class="hover:text-primary-evergreen transition-colors font-medium text-primary-volcanic">Контакти</a>
                    </div>
                </div>

                <a href="/contact" class="hidden sm:inline-block btn-primary">
                    Запази час
                </a>
                
                <button id="mobile-menu-toggle" class="lg:hidden flex flex-col justify-center items-center w-8 h-8 relative">
                    <span class="burger-line w-6 h-0.5 bg-primary-volcanic transition-all duration-300 ease-in-out mb-1"></span>
                    <span class="burger-line w-6 h-0.5 bg-primary-volcanic transition-all duration-300 ease-in-out mb-1"></span>
                    <span class="burger-line w-6 h-0.5 bg-primary-volcanic transition-all duration-300 ease-in-out"></span>
                </button>
            </nav>
        </header>

        <!-- Mobile Menu -->
        <div id="mobile-menu" class="fixed inset-0 top-20 bg-white z-40 transform translate-x-full transition-transform duration-300 ease-in-out lg:hidden overflow-hidden">
            <div class="h-full flex flex-col">
                <div class="flex-1 overflow-y-auto p-6">
                    <nav class="flex flex-col space-y-4">
                        <a href="/" class="block text-lg font-semibold text-primary-volcanic hover:text-primary-evergreen transition-colors py-3 px-4 bg-primary-sand/20 rounded-lg">Начало</a>
                        <a href="/za-men/" class="block text-lg font-semibold text-primary-volcanic hover:text-primary-evergreen transition-colors py-3 px-4 bg-primary-sand/20 rounded-lg">За мен</a>
                        
                         <!-- Services section -->
                        <div class="accordion-section">
                            <button class="mobile-accordion-toggle w-full text-left flex items-center justify-between text-lg font-semibold text-primary-volcanic hover:text-primary-evergreen transition-colors py-3 px-4 bg-primary-sand/20 rounded-lg mb-2" data-target="services-list">
                                <span>Услуги</span>
                                <svg class="w-4 h-4 transition-transform duration-300 ease-in-out" viewBox="0 0 20 20" fill="currentColor">
                                    <path fill-rule="evenodd" d="M5.23 7.21a.75.75 0 011.06.02L10 10.94l3.71-3.71a.75.75 0 111.06 1.06l-4.24 4.24a.75.75 0 01-1.06 0L5.21 8.29a.75.75 0 01.02-1.08z" clip-rule="evenodd"/>
                                </svg>
                            </button>
                            <div id="services-list" class="accordion-content max-h-0 overflow-hidden transition-all duration-500 ease-in-out">
                                <div class="p-4 bg-primary-sand/10 rounded-lg space-y-3">
                                    <a href="/uslugi/" class="block text-md text-primary-evergreen font-semibold hover:text-primary-volcanic transition-colors">→ Виж всички услуги</a>
                                    <div class="space-y-2">
                                        {% for s in services %}
                                        <a href="/uslugi/{{ s.slug }}/" class="block text-base text-primary-deepsea font-medium hover:text-primary-evergreen transition-colors py-1">{{ s.name }}</a>
                                        {% endfor %}
                                    </div>
                                </div>
                            </div>
                        </div>
                        
                        <!-- Products section -->
                        <div class="accordion-section">
                            <button class="mobile-accordion-toggle w-full text-left flex items-center justify-between text-lg font-semibold text-primary-volcanic hover:text-primary-evergreen transition-colors py-3 px-4 bg-primary-sand/20 rounded-lg mb-2" data-target="products-list">
                                <span>Продукти</span>
                                <svg class="w-4 h-4 transition-transform duration-300 ease-in-out" viewBox="0 0 20 20" fill="currentColor">
                                    <path fill-rule="evenodd" d="M5.23 7.21a.75.75 0 011.06.02L10 10.94l3.71-3.71a.75.75 0 111.06 1.06l-4.24 4.24a.75.75 0 01-1.06 0L5.21 8.29a.75.75 0 01.02-1.08z" clip-rule="evenodd"/>
                                </svg>
                            </button>
                            <div id="products-list" class="accordion-content max-h-0 overflow-hidden transition-all duration-500 ease-in-out">
                                <div class="p-4 bg-primary-sand/10 rounded-lg space-y-3">
                                    <a href="/produkti/" class="block text-md text-primary-evergreen font-semibold hover:text-primary-volcanic transition-colors">→ Виж всички продукти</a>
                                    <div class="space-y-2">
                                        {% for product in products %}
                                        <a href="/produkti/{{ product.meta_title }}/" class="block text-base text-primary-deepsea font-medium hover:text-primary-evergreen transition-colors py-1">{{ product.title }}</a>
                                        {% endfor %}
                                    </div>
                                </div>
                            </div>
                        </div>
                        
                        <a href="/kontakti" class="block text-lg font-semibold text-primary-volcanic hover:text-primary-evergreen transition-colors py-3 px-4 bg-primary-sand/20 rounded-lg">Контакти</a>
                    </nav>
                </div>
                
                <!-- CTA Button - Fixed at bottom -->
                <div class="border-t border-primary-dune/20 p-6">
                    <a href="/contact" class="btn-primary w-full text-center">Запази час</a>
                </div>
            </div>
        </div>

        <!-- Main Content -->
        <main class="pt-20">
            {{ content | safe }}
        </main>

        <!-- Footer -->
        <footer class="bg-primary-volcanic text-primary-sand mt-20">
            <div class="max-w-7xl mx-auto py-16 px-4">
                <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
                    <div class="col-span-1 md:col-span-2">
                        <h3 class="text-2xl font-bold mb-4 font-['Playfair_Display']">Вега Тест</h3>
                        <p class="text-white/90 mb-4 max-w-md">
                            Холистична диагностика за здравословно хранене с най-подходящите храни за вашия организъм.
                        </p>
                        <div class="flex space-x-4">
                            <a href="#" class="text-white/80 hover:text-white transition-colors">
                                <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24">
                                    <path d="M24 4.557c-.883.392-1.832.656-2.828.775 1.017-.609 1.798-1.574 2.165-2.724-.951.564-2.005.974-3.127 1.195-.897-.957-2.178-1.555-3.594-1.555-3.179 0-5.515 2.966-4.797 6.045-4.091-.205-7.719-2.165-10.148-5.144-1.29 2.213-.669 5.108 1.523 6.574-.806-.026-1.566-.247-2.229-.616-.054 2.281 1.581 4.415 3.949 4.89-.693.188-1.452.232-2.224.084.626 1.956 2.444 3.379 4.6 3.419-2.07 1.623-4.678 2.348-7.29 2.04 2.179 1.397 4.768 2.212 7.548 2.212 9.142 0 14.307-7.721 13.995-14.646.962-.695 1.797-1.562 2.457-2.549z"/>
                                </svg>
                            </a>
                            <a href="#" class="text-white/80 hover:text-white transition-colors">
                                <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24">
                                    <path d="M22.46 6c-.77.35-1.6.58-2.46.69.88-.53 1.56-1.37 1.88-2.38-.83.5-1.75.85-2.72 1.05C18.37 4.5 17.26 4 16 4c-2.35 0-4.27 1.92-4.27 4.29 0 .34.04.67.11.98C8.28 9.09 5.11 7.38 3 4.79c-.37.63-.58 1.37-.58 2.15 0 1.49.75 2.81 1.91 3.56-.71 0-1.37-.2-1.95-.5v.03c0 2.08 1.48 3.82 3.44 4.21a4.22 4.22 0 0 1-1.93.07 4.28 4.28 0 0 0 4 2.98 8.521 8.521 0 0 1-5.33 1.84c-.34 0-.68-.02-1.02-.06C3.44 20.29 5.7 21 8.12 21 16 21 20.33 14.46 20.33 8.79c0-.19 0-.37-.01-.56.84-.6 1.56-1.36 2.14-2.23z"/>
                                </svg>
                            </a>
                        </div>
                    </div>
                    
                    <div>
                        <h4 class="text-lg font-semibold mb-4">Контакти</h4>
                        <div class="space-y-2 text-white/90">
                            <p>гр. Пловдив</p>
                            <p>Хотел SPS, ет.13, офис 1302</p>
                            <p>тел: 0885 415 300</p>
                            <p>e-mail: e_hrisi@abv.bg</p>
                        </div>
                    </div>
                    
                    <div>
                        <h4 class="text-lg font-semibold mb-4">Работно време</h4>
                        <div class="space-y-2 text-white/90">
                            <p>Понеделник - Петък</p>
                            <p>9:00 - 18:00</p>
                            <p>Събота</p>
                            <p>9:00 - 14:00</p>
                        </div>
                    </div>
                </div>
                
                <div class="border-t border-white/20 mt-12 pt-8 text-center text-white/70">
                    <p>&copy; 2024 Вега Тест. Всички права запазени.</p>
                </div>
            </div>
        </footer>

        <script>
            // Header scroll effect
            window.addEventListener('scroll', function() {
                const header = document.getElementById('header');
                if (window.scrollY > 50) {
                    header.classList.add('bg-primary-sand/95', 'border-primary-dune/20');
                    header.classList.remove('bg-transparent', 'border-transparent');
                } else {
                    header.classList.remove('bg-primary-sand/95', 'border-primary-dune/20');
                    header.classList.add('bg-transparent', 'border-transparent');
                }
            });

            // Drag-to-scroll for horizontal lists
            (function() {
              const containers = document.querySelectorAll('.drag-scroll');
              containers.forEach((el) => {
                let isDown = false;
                let startX = 0;
                let scrollLeftStart = 0;

                const onDown = (clientX) => {
                  isDown = true;
                  startX = clientX;
                  scrollLeftStart = el.scrollLeft;
                  el.classList.add('dragging');
                };
                const onMove = (clientX) => {
                  if (!isDown) return;
                  const dx = clientX - startX;
                  el.scrollLeft = scrollLeftStart - dx;
                };
                const onUp = () => {
                  isDown = false;
                  el.classList.remove('dragging');
                };

                // Mouse events
                el.addEventListener('mousedown', (e) => {
                  // left button only
                  if (e.button !== 0) return;
                  onDown(e.clientX);
                  e.preventDefault();
                });
                window.addEventListener('mousemove', (e) => onMove(e.clientX));
                window.addEventListener('mouseup', onUp);

                // Touch events
                el.addEventListener('touchstart', (e) => onDown(e.touches[0].clientX), { passive: true });
                window.addEventListener('touchmove', (e) => onMove(e.touches[0].clientX), { passive: true });
                window.addEventListener('touchend', onUp);
              });
            })();

            {# // Equalize description heights
            (function(){
              function equalizeDescriptions() {
                document.querySelectorAll('.drag-scroll').forEach(row => {
                  const descriptions = row.querySelectorAll('.service-card-description');
                  if (!descriptions.length) return;

                  // Reset heights to get natural size
                  descriptions.forEach(desc => { desc.style.height = 'auto'; });
                  
                  // Force reflow before measuring
                  void descriptions[0].offsetHeight;

                  let maxHeight = 0;
                  descriptions.forEach(desc => {
                    maxHeight = Math.max(maxHeight, desc.offsetHeight);
                  });

                  descriptions.forEach(desc => {
                    desc.style.height = `${maxHeight}px`;
                  });
                });
              }
              
              window.addEventListener('load', equalizeDescriptions);
              window.addEventListener('resize', equalizeDescriptions);
              const ro = new ResizeObserver(equalizeDescriptions);
              document.querySelectorAll('.drag-scroll').forEach(el => ro.observe(el));
            })(); #}
        </script>
        <script src="{{ '/assets/js/tooltips.js' | url }}"></script>
    </body>
</html>